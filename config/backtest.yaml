# ==========================================
# 🔷 데이터 파이프라인 설정
# ==========================================
data:
  db_path: "data/database/stocks.db"
  ticker_codes:
    - "005930.KS"    # 삼성전자
    - "000660.KS"    # SK하이닉스
    - "051910.KS"    # LG화학
    - "207940.KS"    # SM Entertainment
  start_date: "2024-01-01"
  end_date: "2024-12-31"
#  start_date: "2025-01-01"
#  end_date: "2025-11-30"
  #백테스트 과정에 쓰이는 모든 지표 리스트 (OHLCV 제외)
  indicator_list:
    - "ma_5"
    - "ma_20"
    - "ma_200"
    - "macd"

# ==========================================
# 🔶 모델 학습 설정
# ==========================================
ml:
  model_path: "models/momentum_model.pkl"

# ==========================================
# 🔶 백테스트 설정
# ==========================================
backtest:
  basic_params:
    # 초기 자본금
    initial_cash: 100000000  # 1억원
    # 거래 비용
    commission: 0.00015       # 0.015% (키움증권 기준)
    slippage: 0.0025           # 0.25% (종가 오차 포함)
    leverage: 1.0             # 없음

  # 전략 파라미터
  strategy_params:
    # 보유 기간
    holding_period: 5       # 보유 기간 (일)
    # 각 종목당 투자 비중
    position_weight: null
    # 손절 설정
    use_stop_loss: true
    stop_loss_pct: 0.05     # 5%
    # 익절 설정
    use_take_profit: false
    take_profit_pct: 0.10   # 10%
    # 최대 동시 포지션 (null = 제한 없음)
    max_positions: null
    # 로그 설정
    printlog: true


# ==========================================
# 공통 설정
# ==========================================

# 로그 레벨 설정
log_level: "INFO"

# 출력 설정
plot_path: "data/backtest/backtest_chart.png"
output_csv: "data/backtest/backtest_results.csv"